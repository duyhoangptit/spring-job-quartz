spring:
  application:
    name: system-report-job-test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  quartz:
    job-store-type: jdbc
management:
  endpoints:
    web:
      exposure:
        include: health
